---
layout: post
title: "vps dd重装系统"
date: 2017-12-25
category: 未分类
---

## 前言

有些vps服务商没有提供想要的系统版本是可以通过ssh链接直接dd一个自己想要的系统   或者想要一个更为干净纯净的系统时**此系统为萌卡制作
注意:
全自动安装默认root密码:Vicer,安装完成后请立即更改密码.
请使用 passwd root 命令更改密码.
在安装过程中需要选择是否支持vnc
特别注意:OpenVZ构架不适用.

## 前提需要

需要:
1.Debian/Ubuntu/CentOS 系统(由GRUB引导)；
2.wget 用来下载文件，获取公网IP;
3.ip 获取网关，掩码等;
4.sed awk grep 处理文本流；
5.VNC 安装系统(此项为可选)。
确保安装了所需软件:**

```
#Debian/Ubuntu:
apt-get install -y gawk sed grep
 
#RedHat/CentOS:
yum install -y gawk sed grep
```

**如果出现了错误,请运行:**

```
#Debian/Ubuntu:
apt-get update
 
#RedHat/CentOS:
yum update
```

## 下载使用

一键下载及使用:

```
wget --no-check-certificate -qO DebianNET.sh 'https://moeclub.org/attachment/LinuxShell/DebianNET.sh' && chmod -x DebianNET.sh

备份地址  www.wunail.com/wp-content/uploads/2017/DebianNET.sh
```

使用

```
Usage:
        bash DebianNET.sh       -d/--debian [dist-name]
                                -u/--ubuntu [dist-name]
                                -v/--ver [32/i386|64/amd64]
                                -apt/--mirror
                                -dd/--image
                                -a/-m
```

全自动/非全自动示例:

全自动安装:

bash DebianNET.sh -d wheezy -v i386 -a

VNC手动安装:

bash DebianNET.sh -d wheezy -v i386 -m

使用示例:

【默认】安装Debian 7 x32：

bash DebianNET.sh -d wheezy -v i386

bash DebianNET.sh -d 7 -v 32

安装Debian 8 x64：

bash DebianNET.sh -d jessie -v amd64

bash DebianNET.sh -d 8 -v 64

安装Debian 9 x64：

bash DebianNET.sh -d stretch -v amd64

bash DebianNET.sh -d 9 -v 64

安装Ubuntu 14.04 x32：

bash DebianNET.sh -u trusty -v 32

安装Ubuntu 14.04 x64：

bash DebianNET.sh -u trusty -v 64

安装Ubuntu 16.04 x64：

bash DebianNET.sh -u xenial -v 64

安装Ubuntu 17.04 x64：

bash DebianNET.sh -u zesty -v 64

转载[Debian(Ubuntu)网络安装/重装一键脚本](https://moeclub.org/2017/03/25/82/)
**鸿鹄博客 , 版权所有丨如未注明 , 均为原创丨本网站采用[BY-NC-SA](http://creativecommons.org/licenses/by-nc-sa/3.0/)协议进行授权 , 转载请注明[vps dd重装系统](http://www.wunail.com/archives/546)！		      

			[喜欢 (3)](javascript:;)赏[aa@qq.com]![](http://www.wunail.com/wp-content/uploads/2016/08/zfb.jpg)**分享 (0)[](#)[](#)[](#)[](#)[](#)[](#)[](#)
